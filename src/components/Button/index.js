import React from 'react'
import './styles.css'

/**
 * TODO: Implement the button which should be a simple UI component
 */
export const Button = ({ flavor }) =>
  <button className={`App-button ${flavor}`}>
    0
  </button>
